# motbuchanan.com — ROADMAP

Self-documenting record of this repo. Fetch this file (raw) before any work and check it against what's actually in the repo. Update it with every upload and ship it in the same zip as the change it documents.

## What this is
The "This Phone" portfolio at motbuchanan.com: a phone-shaped home screen of single-file HTML apps, hosted on GitHub Pages from repo motbuchanan/site. Temporary front door that Mot now intends to keep (The Closet concept likely abandoned in favor of this).

## Current version
- index.html footer: **v7.34 · Sep 22** (all three stamps: footer tagline, Settings updBadge, About verNum/verNum2)
- buildlog.html: log r8 · Sep 22
- Deployed authoritative source = this repo. Every zip in Downloads is behind it.
- NOTE: ROADMAP was stale from v7.15 to v7.27 (Aug 24 to Sep 6). Re-synced Sep 21. Do not let it drift again: update with every upload.

## What shipped (newest first)
- v7.34 (Sep 22): BLOOM wired in. New file lab/bloom.html (~23 KB), an L-system plant generator: sliders (fullness, spread, height, droop, flower size, density) seed a plant that grows one step per frame, forking and bursting into flower sprays; teal-to-pink color ramp with opacity depth shading; five presets; tap-to-plant; save-frame-as-image. data-cat=make, data-game="lab/bloom.html" (LOCAL copy, not an external-URL tile). Card copy drafted (verb "Grow a plant"). LAYOUT (corrected per Mot Sep 22 19:25): MAKE folder holds {Pigment, Bloom} only; NAZCA STAYS LOOSE in its v7.33 spot (row 1, col 3). 7 loose + Play + Learn + Make + Triangle + Measure Up = exactly 12 tiles on one page, Measure Up bottom-right. Page-1 grid (4x3): row1 All Thumbs / The Keep / Nazca / Haunt Inc; row2 Reframe / LiftSafe / Knight School / Play; row3 Learn / Make / Triangle / Measure Up. (First pass wrongly folded Nazca+Pigment+Bloom all into Make; reverted.) NEW badge stays on The Keep. Bloom ICON: Mot supplied a generated image (teal glowing stems fanning from a bright base, coral/pink flower sprays on black); embedded base64 (256px JPEG ~11.7 KB) replacing the interim inline-SVG placeholder. Storage stat still 19 apps (folder members park in #stash when closed). Tested green: page order, Make folder contents, Bloom card + launch, 0 console errors.
- v7.33 (Sep 22): Haunt Inc. wired in as an external-URL tile (https://motbuchanan.github.io/gameshelf/hauntinc/, like Measure Up), icon embedded base64, data-cat play, card copy drafted. Home reorg per Mot: top row now All Thumbs, The Keep, Nazca, Haunt Inc; Knight School dropped to the old Star Stuff slot; Star Stuff moved into the Play folder (8 games now). NEW badge stays on The Keep only. PREREQUISITE shipped in the gameshelf repo: hauntinc v4.9 and measureup v0.99 now hide their in-game "Shelf" exit button when embedded in an iframe (window.self!==window.top), so a site visitor can't fall through to the family Game Shelf; measureup sw cache bumped to v0-99.
- v7.32 (Sep 22): Two corrections to v7.31 per Mot. NEW badge moved off Measure Up onto THE KEEP (the app he is actively developing). SCREEN TILT / PARALLAX REMOVED entirely (he unchecked it in the lab; his export listed only squish, time of day, new badge). The data-flow grid backdrop, tap ripple, time-of-day tint and tap squish all stay. Backdrop now draws at a fixed grid position (no tilt offset). Perf note: this also drops the per-frame wall/grid/widget transform writes.
- v7.31 (Sep 22): NEW HOME SCREEN, built from Mot's Layout Lab result. FOLDERS layout: 8 loose on top (All Thumbs, The Keep, Nazca, Knight School, Reframe, LiftSafe, Pigment, Star Stuff), a Play folder (7) and Learn folder (SHARP, HARP) and Triangle alone on the bottom row, Measure Up pinned bottom-right with a NEW badge. Tapping a folder opens a blurred pop of its apps; tapping an app there opens its CARD (the v7.28 sheet), same as a loose tile. BACKDROP: a data-flow circuit grid drawn on a canvas behind the icons (category colors, his slider values amount 1.1 / speed 0.3 / size 0.9 / glow 0.7), with a tap-ripple that lights the lines. MOTION: whole screen parallax-tilts on pointer/gyro (strength 1.8), a time-of-day tint over the wall off the visitor's real clock, and a squish (jelly) on every tile tap. New proper GEAR icon in the dock (toothed, replaces the plain asterisk gear). BUILD LOG tile removed from the grid; the site changelog lives in Settings > Software Update (the CHANGES mirror), each app keeps its own history in its card. Storage stat stays stable at 19 apps because folder members park in a hidden #stash when their folder is closed. All launch/card/settings/camera/lock paths re-tested green, 0 console errors. Implementation notes: TILES map + ITEMS array drive the fold pager; folder pop pushes history so one Back closes it; openG still reuses the card's history entry so one Back from an app returns home.
- v7.30 (Sep 21): MEASURE UP wired in as the site's first EXTERNAL-URL tile (data-game = https://motbuchanan.github.io/gameshelf/measureup/, loads the live Game Shelf build in the app viewer, never copied here). data-cat = learn (Mot's handoff said play; learn matches the teaching-first category the other trainers use, flagged to him). Card: verb "Lead the band", copy from his MEASUREUP-FOR-SITE.md handoff (drafts for his voice pass), gallery = shot-measureup-1.jpg (band lineup) + shot-measureup-2.jpg (title). Card file-size now shows "live" when a cross-origin fetch cannot be measured instead of hanging on "...". Placed on page 2 after The Upcard pending his Layout Lab verdict. Build log entry added.
- LAYOUT LAB (Sep 21, not linked from the site): lab/layout-lab.html. Phone mock with all 20 real icons. Arrange mode (drag, tap-to-swap, long-press, set category), 5 layouts (pages / featured widget / page per kind / folders / App Library), 7 color-coding styles x 3 palettes, attention overlays (eye heat, right-thumb reach, slot ranking), 9 motion samples (settle, squish, glint, foil widget, depth tilt, drifting light, breathing light, dot-matrix tagline, NEW pulse). Send tab produces plain text Mot pastes back; next site ship builds from that text.
- v7.29 (Sep 21): SHARED CHROME PASS across the apps. Spec = CHROME.md at repo root (tokens, type, home button, version badge, buttons, pills, panels). Each app keeps its content world (boards, felt, space, paper, hazard stripes); chrome is now identical. Touched: knightschool v1.1, sudoku v2.1, jigsaw v1.1, brickbreak v1.1, boardgames v1.1, cards v1.1, buildlog v1.1, starstuff v6.1.1, pigment v1.5.1, thekeep v0.7.12, production-triangle v10.1, liftsafe v3.0.4, allthumbs hub v1.17 (chapters untouched). Every app now has the .mb-ver badge bottom-right (tap = "vX.Y loaded" toast). Self-back hubs use the shell's "← home" pill. Card status strings in index APPINFO synced (Keep, Reframe, Nazca, LiftSafe). Not touched, already on brand: sharp, harp, upcard, fiveletters, reframe/, nazca/. Known: LiftSafe course copy still holds \u2014 escapes inside content strings (copy pass, not chrome).
- v7.28 (Sep 21): APP CARDS. Tapping a home-grid icon no longer launches the app. It raises a bottom-sheet card: icon, name, category chip, status line, one-line "what it is", a big per-app verb button (Learn chess / Walk the Keep / Start drawing...), three stats (file size measured live by fetching the app's files, file count, month started), a horizontal screenshot gallery (tap to enlarge), then Why I made it / How it came together / What I learned. Swipe down, X, scrim tap, or Back closes it. The verb button launches the app through the existing openG() and REUSES the card's history entry, so one Back from an app returns straight home (no extra back press). Dock Camera still launches directly. All 19 grid apps have a card. Copy = DRAFTS pulled from build notes, for Mot to rewrite in his own voice (the APPINFO object in index.html holds every card's text; edit there). Screenshots ship as flat root files shot-<app>-N.jpg (19 files, 480px wide, ~1.4 MB total); The Keep uses its own thekeep/0N-*.jpg dev-phase shots. COLOR CODING added: five categories, each tile label carries a small colored dot and the card's chip + button take the same color. play = teal #4cf0e0, learn = orange #f59847, tool = green #7be495, make = purple #b57cff, meta = grey #8a94a3. Settings > Storage auto-counts the same five. Tile data-cat values changed: Knight School, All Thumbs, SHARP, HARP, LiftSafe are now "learn". Dots can be hidden site-wide with class "nocc" on <html> if Mot decides against them. Also: buildlog.html got entries for Aug 27 (Upcard/SHARP/HARP), Sep 6 (Nazca), Sep 21 (cards) and its "Sixteen games" line corrected to thirteen; two home taglines now point at tapping an icon.
- v7.27 (Sep 6): Nazca tile (nazca/ folder), Sudoku moved to page 2, HARP art recropped, shell back button moved into its own 48px strip above the app iframe.
- v7.26 to v7.16 (Aug 27 to Sep 1): swipe pager (12 per page), search bar removed, The Upcard tile, SHARP + HARP tiles, The Keep tile (thekeep/ folder), grid reorder.
- v7.15 (Aug 24): FINAL camera icon - Mot designed it himself in camera-studio.html and supplied the exact SVG, installed verbatim into the dock. Rounded body + hump + flash, his WHOLE unmodified mark as the lens (scale 1.19 body, mark ~53px). Also fixed the studio: earlier versions cropped the mark to "just the blades" and drew a fake closed ring, which destroyed the mark's real top-of-ring GAP where the camera-body cap meets it - the mark is ONE path (9 subpaths: cap + gapped aperture) and must NEVER be cropped or have a ring added. camera-studio.html kept as a reusable icon-design tool (not a site page). LESSON LOGGED: use the whole mot-aperture.svg path as one unit; do not reconstruct it.
- v7.14 (Aug 24): [superseded] Camera icon redrawn to line-art to match its dock neighbors (mail + brightness are hollow stroked, weight ~4). Prior v7.13 was solid-filled and read as a dark blob at true dock size. Now: hollow stroked camera body + viewfinder hump + flash dot, aperture mark sits SMALL inside its own lens ring (viewBox crop "965 210 625 625", ~20px in the 96 tile) with real breathing room. LESSON: cairosvg previews at 288px looked fine but the icon failed at the real ~60px dock size - always render icons at true small size (60px) before shipping, and match the surrounding icons visual language (line vs fill).
- v7.13 (Aug 24): [superseded] solid-filled camera body attempt - traditional camera silhouette (rounded body, viewfinder hump on top, flash dot) with Mot's real aperture as the LENS cropped to just the blades (source viewBox crop "965 210 625 625", cap excluded so the body's own hump reads as the viewfinder). Chosen from 3 variants; the mark-alone version was only 80% ("camera" cue too weak). Lens inlined, render-verified from file.
- v7.12 (Aug 24): [earlier] Dock Camera icon showed Mot's real aperture mark (was a generic camera-body + plain-circle lens). Presented as the mark alone on the dark teal tile (chosen over a camera-body-with-lens version, which double-framed since the mark already has the body cap). IMPORTANT rendering lesson: <use href="#apmark"> did NOT resolve when nested inside an inner <svg> with a different viewBox (rendered invisible) - so all three mark instances (dock, lock, About) now INLINE the aperture path directly, and the shared <symbol> was removed. To place the mark anywhere new, inline this wrapper: <svg viewBox="940 -200 720 880"><g transform="translate(844.9,-213.7) scale(9.2129)"><path d="...aperture path..." fill="#4cf0e0" fill-rule="evenodd"/></g></svg> (full path lives in mot-aperture.svg in the repo). Always render-verify the mark after placing it.
- v7.11 (Aug 24): Replaced the traced mark with Mot's ACTUAL aperture from his source file mot-aperture.svg (he supplied it after the v7.10 trace was wrong). Now defined ONCE as an inline SVG <symbol id="apmark"> (source viewBox 940 -200 720 880) and referenced via <use href="#apmark" xlink:href> on the lock screen, Settings About page, and camera fallback. Camera photo watermark stamps the same real SVG as a data-URI image onto the canvas. og.png regenerated with it. mot-aperture.svg saved to repo root as the canonical asset. To use the mark anywhere new: <use href="#apmark" .../> (symbol is already in the DOM). DO NOT re-trace it by eye again.
- v7.10 (Aug 24): [superseded] traced camera mark (camera-body cap + 6-blade swept aperture iris + glow center, teal) now used on the lock screen, the Settings About page, and the camera fallback, replacing the earlier plain 4-tick aperture ring. og.png regenerated with the real mark too. SVG is hand-built on a 0..100 viewBox (cap path "M31 30 h8 l4.5 -6..."); reuse that block for any future spot that needs the mark. The wordmark in the .widget header is a separate vector-path SVG (the mark lives inside the "o" glyph) and was NOT touched.
- v7.9 (Aug 23): SETTINGS + CAMERA SHIPPED (big one). New dock = Settings gear + Camera + Mail (About moved off the grid into Settings). Settings is a shell OVERLAY inside index.html (not an iframe) so themes can skin the phone; games are iframes and structurally immune. Rows: About this phone (bio + joke device rows + tappable version), Software Update (changelog mirror, "up to date"), Display (11 themes: Classic/OLED/Glitch/8-Bit/DOS/Cartoon/Blueprint/CRT/Broken/Terminal/Vapor + accent swatches), Language, Sounds/Reduce Motion toggles, Storage (AUTO-COUNTS tiles by data-cat, never hand-edited), Privacy (honest no-tracking statement), Airplane Mode + Say hi. Every grid tile now carries data-cat (play/tool/make/meta) — REQUIRED for Storage; new tiles must include it. Jokes in 3 tiers: visible rows (Model MOT-1/Made in Akron/spite&coffee), tap-to-find (battery drains, airplane toasts "Still here", reset "just kidding"), and a 7-TAP version egg unlocking Developer Options (Balloon physics, Overclock, Show FPS, Reduce Emotion). Language = 3 hard-coded real settings-page translations (es/fr/de, with a "only had time for settings" wink) + 6 pure-JS joke languages (Pirate/Emoji/SHOUTING/1337/Reverse/Binary). Camera = camera.html, real getUserMedia, aperture+motbuchanan.com watermark on capture, graceful fallback if denied/http; iframe allow-list now includes "camera". Prefs persist via guarded localStorage (theme/accent/motion/lang/dev). Themes skin shell only, verified no rule touches iframe/#gf/#player. about.html file left in repo but no longer linked (bio now canonical inside Settings).
- v7.8 (Aug 23): Lock screen SHIPPED. Site opens on a lock screen (live time + date, aperture mark, name, one line). Dismiss: swipe up (touch), click / scroll-down / any key (desktop). Mode = once-then-remembered: guarded localStorage 'seen' flag, returning visitors skip straight to home. Tap the status-bar clock to re-lock. Flip const LOCK_EVERY_VISIT=true to greet every visit. No audio. Uses existing --sys system-font stack and the shared tick clock.
- v7.7 (Aug 23): Home grid reordered range-first (bundled with the still-un-deployed v7.6 SEO changes, so this one upload lands both). Row 1 = Board Games, Pigment, Reframe, Star Stuff (proof / eye-candy / range / anchor). Sudoku and Jigsaw moved late (Mot least proud of them). Board Games promoted for its chess engine. Game files did NOT move; only tile order in index.html changed. Icon color-coding was CONSIDERED and DROPPED: the real Grok icons are an asset worth more than a category-hue system; order carries the grouping instead.
- v7.6 (Aug 23): SEO pass. Real title, search-length description, full Open Graph + Twitter card tags, canonical URL, og.png preview image (dark ground, teal aperture nod), one visually-hidden crawlable h1. No visual change to the phone. No professional/job claim (stealth search preserved).
- v7.5 (Aug 20): tagline rewrite, all "coming soon" language removed, present tense.
- v7.x (Aug 20): card room complete (Poker, Hold'em, Rummy, Tonk, Hearts, Spades, Euchre); Sudoku rebuilt; LiftSafe, Production Triangle, Reframe, Pigment live.
- Aug 14-16: launch, board/card games, About page, Build Log, Jigsaw, Five Letters.

## Locked decisions (do not reopen)
- CHROME.md is the standing chrome spec for every app on this site. New apps ship with it; edits to old apps keep it. (Sep 21)
- Tapping an icon opens its CARD, not the app. The card's verb button is the only launcher for grid apps. (Sep 21)
- Card copy is written in Mot's first-person voice, for a stranger. No employer, no coworkers, no job search, no son's name.
- Screenshots are real captures of the running app, never mockups. Flat root files, shot-<app>-N.jpg.
- File size on a card is measured live (fetch + blob size), never typed in.
- No resume link, no loud professional identity line on the home screen. Stealth job search; Mot's Yanke orbit reaches this domain via his business card.
- No Yanke work shown here (patient reels, brand systems). That content is Yanke-owned and approval-gated.
- Dock is deliberately label-free (real iOS docks are). Do not add a visible text label under one dock icon.
- Real wordmark/aperture used verbatim. Never rebuild the logo.
- No em dashes anywhere, including meta tags. No cream/warm-light backgrounds.
- Card backs: glow icon-only, rings unfiltered. Standard deck only, no cribbage.

## Open queue
00. DECIDED Sep 21 (round 2): the Build Log icon leaves the grid. The FULL site changelog stays, tucked into Settings as a site development log (Software Update page carries it; nothing is lost). Each app card keeps its own version history. Layout Lab v2.1 in lab/ has the 8-loose folders, 18 backdrops incl. 5 grid variants + contours, tap reactions on every layer, tilt depth slider, folder in/out editing.
00b. Layout Lab v2.3 (Sep 22): CORRECTED the Measure Up placement. Mot wanted it ADDED to the open bottom-right slot, not swapped for Star Stuff. Star Stuff restored to the loose 8; Measure Up is now a PINNED STANDALONE icon that sits after the folders (S.pin=['measureup']), landing bottom-right of page 1 next to Triangle, keeping its NEW badge. Learn folder = SHARP + HARP. Storage key bumped to mb_layoutlab_v3 so the corrected default actually shows over an old saved state. Dragging or foldering a pinned app auto-unpins it. Folder-open drift fix from v2.2 kept.
00b0. Layout Lab v2.2 (Sep 22): FIXED the folder-open bug for real. A phone tap drifts a few pixels; the old code killed a folder tap past 8px so a less-clean tap did nothing (Learn especially). Folder-tap tolerance now 26px, app-tap 14px. Folders show a small right-triangle so they read as openable. Default now opens on Mot's round-2 result with Measure Up moved to loose slot 8 (bottom-right, keeps its NEW badge); Star Stuff fell into the Play folder, Learn = SHARP + HARP. Default backdrop = Grid data flow (amount 1.1, speed 0.3, size 0.9, glow 0.7, cat color), motion = squish + time-of-day + new pulse.
00c. AWAITING Mot's next Layout Lab result text: final order, layout mode, color style, and which 1-2 motions become the site's signature. Then ship index v7.31 from it.
0. CARD COPY PASS (next): Mot rewrites each card's why/how/learned in his voice. Drafts with LOW confidence (thin notes): Brick Breaker, Sudoku. MEDIUM: Knight School, Star Stuff, SHARP, HARP, Board Games, Card Games. HIGH: the rest.
0b. UNIFORMITY PASS (after copy): the apps themselves have drifted across chats. Audit Sep 21 (brand = Archivo + JetBrains Mono, teal #4cf0e0, orange #f59847, dark ground):
   ON BRAND: brickbreak, sharp, harp, upcard, boardgames, cards, buildlog, fiveletters, reframe (site colors, system font), nazca (site colors, own paper look on purpose).
   OFF BRAND (own palette, no brand font): The Keep (sand/gold), Knight School + Sudoku (brown/cream/gold, same skin), All Thumbs (blue/purple/green), Pigment (purple/cream), Star Stuff (purple/gold), Production Triangle (gold/green/blue), LiftSafe (safety yellow/grey), Jigsaw (purple-black/gold).
   DONE Sep 21 (v7.29): chrome standardized per CHROME.md, badges everywhere. Remaining judgement calls: Pigment keeps emoji nav icons; Star Stuff keeps emoji mode icons; both are content markers, revisit if Mot wants them gone.
1. Card gallery: dev-phase shots from Mot's flash-drive archive for the apps that have a real history (Knight School, Star Stuff, Pigment, LiftSafe).
2. Split vs mix: home grid stays MIXED (real phones mix). If Mot wants category pages later, the pager can sort by data-cat with one small change.
1. The Upcard (upcard.html) - blackjack play-and-learn, IN BUILD (phased). P1 SHIPPED (upcard v0.2): playable blackjack + chip bankroll + manual sessions + elevated logo card back (aperture on dark-teal, could become shelf-standard) + table felt = Midnight Teal with amber rail (chosen from 2 rounds of samples; scoped to table view only, start screen keeps the site light-ray ground). Bottom-center kept calm for the incoming coach hint strip. P2 SHIPPED (upcard v0.3): coach mode. Basic-strategy engine (S17 multi-deck) unit-tested 270/270 vs canonical chart + 15/15 integration. Default "flag mistakes after" (red correction strip in the protected bottom-center zone), plus "on demand" (ask button) and "warn before" (shows rec pre-move) toggles in a gear settings sheet, and a coach on/off switch. Accuracy now tracked live (sess.decisions/correct) feeding the session summary. NOTE: split still stubbed, so coach evaluates pairs as their hard/soft total for now; split-aware advice ready in the engine for when Phase 3+ enables splitting. IMPORTANT rules verified: hard 11 vs A = HIT (not double) under 6-deck S17; 9,9 stands vs 7/10/A; 5,5 plays as hard 10. JUICE PASS SHIPPED (upcard v0.4): real wide blackjack table (teal felt, orange/amber dealer arc + bet line, tan padded rail, dark floor - built from table-wide.html picker, no seat markers), cards centered and recenter on split later, cards deal in one at a time, hole card flips on reveal, bet puck shows on felt, win/loss/blackjack felt+bank reactions, tactile button presses. Coach bug FIXED: never advises Double/Split on 3+ card or unaffordable hands. Known minor: empty pre-deal betting state has sparse felt (cards fill it on deal) - low priority polish. P3 SHIPPED (upcard v0.5): studyable basic-strategy chart. Full-screen reference (hard totals 8-17, soft A,2-A,9, pairs A,A-2,2), classic color coding (blue Hit / red Stand / orange Double / teal Split = brand). CHART CELLS ARE GENERATED FROM THE ENGINE (via a build step), so chart and coach can never disagree. Opens from a start-screen button and from Settings > Reference. Also: The Upcard is a STANDALONE site app with its OWN home-grid tile + icon (NOT a cards.html entry) - Mot supplied the app icon (upcard-icon.jpg: aperture card back + Ace of spades + branded chips). Wire tile into index.html home grid when the app is DONE. P4 SHIPPED (upcard v0.6): stats dashboard ("Your progress"). KPIs (lifetime net, correct-play %, sessions, hands), two inline-SVG charts (accuracy-over-time in teal, cumulative bankroll growth in green/red, no libraries), W/L/Push summary, per-session list (date/hands/net/accuracy), and an arm-to-confirm reset. Opens from start screen + Settings. All 4 PHASES COMPLETE. Engine 270/270 vs chart, coach 3 modes, chart generated from engine, stats from logged sessions. LESSON: stats cards collided with the playing-card .card class (collapsed to 52px) - renamed to .scard; watch generic class names against the card-game DNA. SPLIT SHIPPED (upcard v0.7): the last core mechanic. Pair splitting fully playable - single player hand becomes a hands[] array (each {cards,bet,done,result,stood}), activeHand index, splitMode flag; hands play in sequence with a teal active-hand highlight, each settles separately vs the dealer, up to 4 hands, re-split supported, split hand can hit/stand/double. Coach now suggests Split correctly (8,8 yes; 5,5 no). All draws routed through a draw() guard (auto-reshuffles empty shoe). Settlement math tested 9/9 (split win/loss, both bust, dealer bust). ALL CORE FEATURES COMPLETE. REMAINING for The Upcard: (1) DONE - split shipped; (2) DONE - The Upcard wired into the home grid (index.html v7.16, aug 27): standalone tile data-cat="play" data-game="upcard.html" with Mot's icon (upcard-icon.jpg resized 256x256 base64), placed after Card Games. It is NOT a selfback app, so the SHELL back button ("home") returns to the grid; The Upcard's own internal "Cards" back link was REMOVED and #bar given left padding (84px) so the title clears the shell back overlay. (3) minor: empty pre-deal betting felt is sparse. P4 = stats dashboard (money + accuracy growth across sessions). Name = "The Upcard", tagline "Play & Learn Blackjack". Not yet on shelf grid. uc_* localStorage namespace.
2. Settings polish backlog: more themes if wanted, wire real Build Log content into Software Update so it can't drift, retire about.html file, optional Wallpaper picker + Text Size slider (designed, not built).
2. og.png is a plain generated placeholder. Replace with a better on-brand image when Mot wants.
3. Optional Build Log entries for v7.5 (taglines) and v7.6 (SEO).
4. Grok cover art for newer tiles if any are still on vector placeholders.
5. Security: verify Firestore rules on the gameshelf public Firebase key.
7. Decide links.html: keep or delete.

## Resolved / dropped
- HTTPS confirmed ticked (Mot verified repeatedly). Cloudflare DNS move DROPPED - no reason, GitHub Pages serves fine. Do not re-suggest either.

## Gotchas
- Large files upload via "Upload files" only, never paste into GitHub's mobile editor (silently truncates).
- Art must be base64-embedded in index.html (relative paths break from Downloads).
- Verify the live site with a hard refresh after deploy; cache can show an old version briefly.

## Re-entry
1. Fetch raw index.html + this ROADMAP before editing.
2. Treat Locked decisions as binding.
3. Upload index.html + og.png + ROADMAP.md together.


## SHARP + HARP wired into site (index v7.16, aug 27)
- Both trainers now have their own home-grid tiles (data-cat="play"), placed after The Upcard, after Card Games. Icons: sharp-icon.jpg (teal/orange piano) + harp-icon.jpg (teal harmonica on orange), Mot-generated, resized 256x256 base64.
- harmonica.html RENAMED to harp.html (matches app name).
- Both got site-integration prep: body padding-top:56px so the shell "home" button clears their headers; neither had an internal back button to remove. NOT selfback apps, so the shell back returns to grid.
- BRAND PASS done (both v0.2): site palette (teal/orange), Archivo type, version badges, all em dashes purged. VISUAL PASS: SHARP piano has depth (recessed keybed, top-lit keys, teal "next" glow, orange press) + a PERMANENT orange felt strip across the top of the keybed (Mot flagged twice that press-only orange was invisible). HARP holes redesigned as recessed metal cavities glowing orange (blow) / teal (draw) from inside a brushed-metal comb (option A restyle, layout preserved). Menus juiced in both.
- Grid now 16 tiles. STILL PENDING: swipe pager (Build Log first on page 2), and SHARP/HARP Phase 2 songbook expansion (PD songs).
